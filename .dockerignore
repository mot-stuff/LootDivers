# Context filter for server/Dockerfile, which builds from the REPO ROOT
# (TASK-717/DEC-043: the API bundles src/core + src/persistence). Exclude
# everything, then re-include exactly what the image build reads.
*
!server/package.json
!server/package-lock.json
!server/tsconfig.json
!server/src
!server/migrations
!src
